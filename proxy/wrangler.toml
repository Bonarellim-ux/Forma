name = "forma-proxy"
main = "worker.js"
compatibility_date = "2024-11-01"

# The API key is stored as an encrypted secret, not here.
# Set it once via: npx wrangler secret put ANTHROPIC_API_KEY
# Then paste your key when prompted. It never appears in code or logs.
